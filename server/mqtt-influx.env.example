# Copy this file to a local path that is NOT committed, for example:
# /home/anthony/.config/homelab/mqtt-influx.env

INFLUX_TOKEN=replace_with_real_token
INFLUX_URL=http://localhost:8181/api/v3/write_lp
INFLUX_DATABASE=homelab
MQTT_BROKER=localhost
MQTT_PORT=1883
